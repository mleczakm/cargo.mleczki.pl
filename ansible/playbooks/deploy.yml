---
- name: Deploy Cargo Application
  hosts: mikrus
  become: yes
  vars_files:
    - config.yml
  tasks:
    - name: Deploy cargo app
      include_role:
        name: app_deploy
      loop: "{{ apps }}"
      loop_control:
        loop_var: app
