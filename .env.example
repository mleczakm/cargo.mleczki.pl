# Local development with Mailpit (copy to .env and adjust as needed)
MAILPIT=1
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_FROM=noreply@cargo.mleczki.pl

# Production email (Brevo) — used when SMTP_HOST and MAILPIT are unset
# BREVO_API_KEY=
# BREVO_SENDER_EMAIL=noreply@cargo.mleczki.pl
